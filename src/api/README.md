# `src/api` (Node handlers)

This folder contains **Node.js route handlers** used by `server.js`.

Current file:

- `newsHandlers.js`: handlers for `/api/news/*` on the local Node server.

This is not a standalone HTTP server package; it is imported by `server.js`.
